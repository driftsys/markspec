[package]
name = "markspec"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["DriftSys"]
license = "MIT"
description = "Typographic theme for MarkSpec — documents, books, and slide decks"
repository = "https://github.com/driftsys/markspec"
compiler = "0.12.0"
categories = ["presentation", "layout"]
keywords = ["slides", "document", "industrial", "requirements", "traceability"]
