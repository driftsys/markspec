/**
 * @module lsp
 *
 * MarkSpec LSP server — two completion modes:
 * 1. New requirement block snippet generation
 * 2. ID reference completion (with doc comment context guard
 *    to prevent collision with language-specific LSP servers)
 */

export const VERSION = "0.0.1";
